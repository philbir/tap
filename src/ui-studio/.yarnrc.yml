# node-modules (not PnP): vite 8's Rolldown bundler panics on Yarn PnP zip paths
# on Windows (pnp crate mangles the C:\ drive path → worker panic → build hangs).
# node-modules gives Rolldown real paths and dodges the bug. Matches src/desktop.
nodeLinker: node-modules
