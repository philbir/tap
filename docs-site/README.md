# Tap Docs Site

Static landing page and documentation site for Tap.

```bash
yarn
yarn dev
yarn build
yarn preview
```

The Vite config uses `base: "./"` so the generated `dist/` works under GitHub Pages project paths such as `https://philbir.github.io/tap/`.
