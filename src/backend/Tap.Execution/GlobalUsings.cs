// The Web SDK hands these to Tap.Studio implicitly. This is a plain class library on purpose
// — that is the whole point of the split — so it names them itself.
global using Microsoft.Extensions.Logging;
