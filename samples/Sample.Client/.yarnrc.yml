# TypeScript 7's native compiler cannot resolve types through Yarn PnP
# (no .pnp.cjs hook in the Go binary). node-modules gives it real paths.
# Matches src/ui-studio, src/desktop and docs-site.
nodeLinker: node-modules
