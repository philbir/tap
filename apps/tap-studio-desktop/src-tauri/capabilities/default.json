{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability set for the main Tap Studio window: launch the sidecar, navigate the webview to its bound URL, and receive OAuth deep-link callbacks.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:webview:allow-webview-show",
    "core:window:allow-set-title",
    "shell:default",
    "deep-link:default",
    "updater:default"
  ]
}
